#run flutter pub run icons_launcher:create --flavor dev
icons_launcher:
  image_path: "assets/images/logo.png"
  platforms:
    android:
      enable: false
    ios:
      enable: false
    web:
      enable: true
    macos:
      enable: true
    windows:
      enable: true
    linux:
      enable: true
